# Async Handling

- use traffic prioritization, delay tolerance to determine how long each request can be delay 
- categorize traffic e.g. daily, major events, and incident response
- optimize machine by batching, use resource-constraints to prevent jobs from exceeding its allocated capacity
